apiVersion: k6.io/v1alpha1
kind: TestRun
metadata:
  name: k6-weekday-test
  namespace: ballandbeer
spec:
  parallelism: 1
  script:
    configMap:
      name: k6-weekday-script
      file: weekday-traffic.js
  arguments: --env BASE_URL=
  cleanup: post
